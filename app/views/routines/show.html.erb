<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Routines</title>
  </head>
  <body>
    <% Habit.all.each do |habit| %>
    <h1><%= habit.routine.capitalize %>:</h1>
    <% end %>
  <h3>Today's Progress</h3>
    <%= form_tag("/routines/#{:id}") do %>
      <input id="completed" type="checkbox" name="" value=""></input>
      Notes: <input type="text" name="notes" value="notes"></input>
      <input type="submit" value="Submit">
      <% end %>
  </body>
</html>
