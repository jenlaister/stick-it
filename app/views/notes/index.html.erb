<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <ul>
      <% @notes.each do |note| %>
      <% Routine.all.each do |routine|  %>
      <% if note.routine_id == routine.id %>
      <li><%= note.comment %></li>
      <% end %>
      <% end %>
      <% end %>
    </ul>
  </body>
</html>
