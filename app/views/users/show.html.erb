<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>User Profile</title>
  </head>
  <body>
    <h1><%= @user.username.capitalize %>'s Profile</h1>
    <ul>
      <h3>Habits:</h3>
      <% @user.habits.each do |habit| %>
      <li><%= link_to "#{habit.routine}" %></li>
    </ul>
    <% end %>
    <%= link_to "Create a New Habit" %><br>
    <%= link_to "Logout", logout_path %>
  </body>
</html>
